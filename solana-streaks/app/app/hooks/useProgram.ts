"use client";

// Simplified hook for demo deployment
// Full Anchor integration will be enabled after smart contract deployment

export function useProgram() {
    // Return null for demo mode - all functionality uses mock data
    return { program: null, provider: null };
}
